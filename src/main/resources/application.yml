server:
  port: 8081
  servlet:
    context-path: /base
# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 初始化数据库脚本
    #    schema: classpath:db/schema-h2.sql
    # 初始化数据脚本
    #    data: classpath:db/data-h2.sql
    url: jdbc:mysql://xxx:3306/test?zeroDateTimeBehavior=convertToNull&characterEncoding=UTF-8&useSSL=false
    username: xxx
    password: xxx